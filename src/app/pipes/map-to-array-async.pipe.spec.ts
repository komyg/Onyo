import { MapToArrayAsyncPipe } from './map-to-array-async.pipe';

describe('MapToArrayAsyncPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToArrayAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
