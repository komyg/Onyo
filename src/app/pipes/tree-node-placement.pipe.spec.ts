import { TreeNodePlacementPipe } from './tree-node-placement.pipe';

describe('TreeNodePlacementPipe', () => {
  it('create an instance', () => {
    const pipe = new TreeNodePlacementPipe();
    expect(pipe).toBeTruthy();
  });
});
